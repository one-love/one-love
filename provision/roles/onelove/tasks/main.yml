---
- name: add onelove service
  become: yes
  template:
    src: onelove.service.j2
    dest: /lib/systemd/system/onelove.service
    owner: root
    group: root
  notify:
    - restart onelove

- name: reload systemd
  become: yes
  command: systemctl daemon-reload

- name: start onelove service
  become: yes
  service:
    name: onelove
    enabled: "{{ onelove_enabled }}"
    state: "{{ onelove_state }}"
